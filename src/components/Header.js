import React from 'react';

const Header = () => (
  <header>
    <nav className="navbar navbar-dark bg-info">
      <a href="/" className="navbar-brand">BlogList</a>
      <span className="navbar-text">built with React</span>
    </nav>
  </header>
);

export default Header;
